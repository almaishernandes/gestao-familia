// Gerado normalmente via: supabase gen types typescript --project-id <id> > src/types/database.types.ts
// Placeholder mínimo — substitua pelo tipo gerado a partir do schema.sql real.
export type Database = Record<string, unknown>;
